---
# Global variables for all inventory groups
# Only essential variables that need to be global should be here

# ServiceNow ITSM Configuration
# Variables are defined directly in vault.yml (no indirection needed)

# Network Device Connection Defaults (timeouts only - connection type is OS-specific)
ansible_command_timeout: 60
ansible_connect_timeout: 30